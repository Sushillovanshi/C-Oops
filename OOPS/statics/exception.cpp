#include <iostream>
using namespace std;
int main()
/*{
    int a, b;
    cout << "enter a 2 vlaue's\n";
    cin >> a >> b;
    try
    { //   try {if (condtion){throw varibal;}code stetment;}
      //    catch (datatyp variable){ statement;}
        if (b == 0)
        {
            throw b;
        }
        cout << a / b << endl;
    }
    catch (int e)
    {
        cout << "denominator should not be zero\n";
    }
    cout<<"ok";
}*/

/*{
    int a []={1,2,3,4,5};
    int s=sizeof(a)/sizeof(a[0]);
    int b;
    cout<<"entera number";
    cin >>b;
    try
    {
        if(b>s-1)
        {
            throw b;
        }
        cout<<a[b]<<endl;
    }
    catch(int e)
    {
        cout<<"out of number\n";
    }
cout <<"ok";
        
}*
// Multiprocessing Otia the multi processing X of multiple software interaction development so that is one multi process we also know multi processing or multi tasking yes only
 